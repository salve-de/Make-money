# Scheduled research staging

This directory is the append-only staging area for scheduled ChatGPT research.

Lanes:
- `backfill/`
- `discovery/`
- `monitor/`
- `evolve/`

Every execution creates a new JSON artifact. Do not use a shared spreadsheet or shared mutable result file.

Read `collection-policy.v1.json` before every run.
