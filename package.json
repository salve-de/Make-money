{
  "name": "goldmine-radar",
  "version": "0.1.0",
  "private": true,
  "description": "Evidence-backed business opportunity discovery database and marketplace.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
